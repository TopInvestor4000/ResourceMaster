apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.sonar.name }}-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: "cinder-csi-ssd"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
